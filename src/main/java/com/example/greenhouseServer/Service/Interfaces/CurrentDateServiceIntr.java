package com.example.greenhouseServer.Service.Interfaces;

import com.example.greenhouseServer.Entity.CurrentDate;

public interface CurrentDateServiceIntr {
    void save (CurrentDate currentDate);
    void update (CurrentDate newCurrentData);
}
